<h1><%= t('headers.events') %></h1>

<div class="my-4">
  <%= link_to t('buttons.create_event'), new_event_path, class: 'btn btn-success' %>
</div>

<div class="row mb-2">
  <%= render partial: 'events/event', collection: @events %>
</div>